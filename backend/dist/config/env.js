"use strict";
// import dotenv from 'dotenv';
// dotenv.config();
// export const env = {
//   PORT: process.env.PORT || 5000,
//   MONGO_URI: process.env.MONGO_URI || '',
//   JWT_SECRET: process.env.JWT_SECRET || 'supersecret',
// };
// import dotenv from 'dotenv';
// import path from 'path';
// console.log('Current directory:', process.cwd());
// console.log('Loading .env from:', path.resolve(process.cwd(), '.env'));
// dotenv.config();
// const required = ['PORT', 'DB_URL', 'JWT_SECRET'];
// required.forEach((key) => {
//   if (!process.env[key]) {
//     throw new Error(`Missing env var: ${key}`);
//   }
// });
// export const env = {
//   PORT: process.env.PORT!,
//   DB_URL: process.env.DB_URL!,
// //   JWT_SECRET: process.env.JWT_SECRET!,
// };
// import dotenv from 'dotenv';
// dotenv.config();
// export const env = {
//   PORT: process.env.PORT || '5000',
//   DB_URL: process.env.DB_URL || '',
//   JWT_SECRET: process.env.JWT_SECRET || '', // ✅ Add this line
// };
// import dotenv from 'dotenv';
// dotenv.config();
// export const env = {
//   PORT: process.env.PORT || '5000',
//   DB_URL: process.env.DB_URL || '',
//   JWT_SECRET: process.env.JWT_SECRET || 'default_secret_key', // fallback key
// };
// // Also export JWT_SECRET separately for easy import
// export const JWT_SECRET = env.JWT_SECRET;
